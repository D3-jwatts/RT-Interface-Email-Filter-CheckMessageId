INSTALLATION
    To install this module, run the following commands:

        perl Makefile.PL
        make
        make install

    Add 'Filter::CheckMessageId' to the list of mail plugins in RT_SiteConfig.pm.

        Plugin( 'RT::Interface::Email::Filter::CheckMessageId' );
        Set( @MailPlugins, 'Filter::CheckMessageId' );

    Restart web server.
